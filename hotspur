#!/bin/bash
function cleanup {
	conda deactivate
}
trap cleanup EXIT

SCRIPT_DIR=$(dirname $(realpath $0))
source "${SCRIPT_DIR}/hotspur_env.sh"

source activate "$HOTSPUR_CONDA_ENV"
python3 "${SCRIPT_DIR}/collection_processor.py" $@
cleanup
